version: '3.8'
services:
  catalogdb:
    image: postgres
  
  basketdb:
    image: postgres
   
  disturbutedcache:
    image: redis

  catalogapi:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/CatalogAPI/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket:


 
